[project]
name = "performance-agent-standards"
version = "0.0.1"
description = "Google Antigravity, Claude Code, and Codex coding standards plugin"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = [
    "prek>=0.1.0",
]
