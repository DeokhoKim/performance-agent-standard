argument-hint: "[target] [standard]"
arguments:
  - name: target
    description: Target file, directory, or git diff to review. Defaults to git diff or current workspace.
  - name: standard
    description: Target C++ standard (cpp17, cpp20, cpp23). Defaults to detected or modern baseline.
agent: general-purpose
